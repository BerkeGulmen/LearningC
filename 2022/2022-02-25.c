#include <stdio.h>


main () {


// Kullanıcıdan aldığın iki sayının çarpımının 7 fazlasını ekrana yazdır
/*int saat, ay;
printf ("lütfen bir sayı giriniz:\n");
scanf ("%d", &saat);
printf ("lütfen ikinci sayıyı giriniz:\n");
scanf ("%d", &ay);
printf ("girinlen sayı = %d\n", ay * saat + 7);*/


// Kullanıcı bir sayı girsin. Bak bakalım 10'dan büyük mü?
/*int yu;
printf ("lütfen bir sayı giriniz:\n");
scanf ("%d", &yu);
if (yu > 10) {
    printf ("yu ondan büyüktür\n");
} else {
    printf ("yu ondan küçüktür\n");
}*/

// Kullanıcının girdiği sayı hem 100'den büyük hem de 200'den küçükse ekrana HARİKA diye yazdır
/*int kes;
printf ("lütfen bir sayı giriniz:\n");
scanf ("%d", &kes);
if (kes > 100) {
    if ( kes < 200) {
        printf ("HARİKA");
    }
}*/








// Kullanıcı, 13 sayısını bulasıya kadar tekrar tekrar sayı girsin.
// 13'ten küçük bir sayı girdiğinde ekrana "küçük sayı girdin" yazsın.
// 13'ten büyük bir sayı girdiğinde ekrana "büyük sayı girdin" yazsın.
// 13 bulunduğunda ekrana HARİKA diye yazsın.
int ya;
printf ("sayı gir:\n");
scanf ("%d", &ya);
while (ya != 13) {

}
/*int as;
while (as != 13) {
    printf("lütfen bir sayı giriniz:\n");
    scanf("%d", &as);
    if (13 > as) {
        printf("küçük sayı girdin");
    }
    if (13 < as) {
        printf("büyük sayı girdin");
    }
    if (13 == as) {
        printf("HARİKA");
    }
}*/






}
/*
Ask the row and column numbers of matrix as "int int"
Get the all double values of matrix as a line as "double double double..."
Find the inverted matrix of the matrix
*/
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (int argc, char * (* argv), char * (* envp)) {
    return EXIT_SUCCESS;
}
